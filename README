A simple HTTP 1.1 server that redirects requests with the GET method to the same
URL but with the HTTPS scheme instead, and drops all other requests.

It can only handle a few connections at a time, and it doesn't rate limit
clients which means that it is easily susceptible to DoS attacks.
